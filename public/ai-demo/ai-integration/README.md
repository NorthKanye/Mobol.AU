# AI integration page — generated image assets

Drop generated campaign images for the `/services/ai-integration` `ImageGenDemo` here.

Expected files (prompts live in the plan and in `ImageGenDemo.tsx`):

- `gen-hero.png` — Aster Lane hero product set
- `gen-flatlay.png` — overhead refillable-skincare flatlay
- `gen-cinematic.png` — cinematic single-bottle campaign frame

Until these exist, `ImageGenDemo` falls back to the existing
`/ai-demo/aster-lane-*.png` assets, so the demo never looks broken.
